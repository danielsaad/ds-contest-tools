name: Publish to PyPI

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

jobs:
  build-and-publish:
    name: Build and Publish Release
    runs-on: ubuntu-latest
    
    # As permissões necessárias para o Trusted Publishing do PyPI (OIDC)
    permissions:
      contents: read
      id-token: write

    steps:
    - uses: actions/checkout@v4
      with:
        # Puxamos todo o histórico para que o setuptools-scm possa ler a tag
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"

    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine

    - name: Build sdist and wheel
      run: python -m build

    - name: Check package with twine
      run: twine check dist/*

    - name: Publish package to TestPyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        repository-url: https://test.pypi.org/legacy/
        # Ignora falhas no TestPyPI caso a versão já exista de testes anteriores
        skip-existing: true
        
    # DESCOMENTE ESTA ETAPA QUANDO FOR PUBLICAR OFICIALMENTE NO PYPI
    # - name: Publish package to PyPI
    #   uses: pypa/gh-action-pypi-publish@release/v1
